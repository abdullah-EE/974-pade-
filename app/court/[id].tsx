import { useLocalSearchParams } from 'expo-router';import { Modal, Pressable, ScrollView, Text, View } from 'react-native';import { courts, matches, players } from '@/data/mockData';import { ImageWithFallback } from '@/components/common/ImageWithFallback';import { PremiumButton } from '@/components/common/PremiumButton';import { useState } from 'react';
export default function CourtDetail(){const {id}=useLocalSearchParams();const c=courts.find(x=>x.id===id)||courts[0];const [m,setM]=useState('');return <ScrollView style={{flex:1,padding:14}}><ImageWithFallback uri={c.image} style={{height:260,borderRadius:16}}/><Text style={{fontSize:24,fontWeight:'800'}}>{c.name}</Text><Text>{c.area} • {c.indoor?'Indoor':'Outdoor'}</Text><Text>{c.timeSlots.join(' · ')}</Text><Text>Amenities: {c.amenities.join(', ')}</Text><Text>Players here tonight</Text>{players.slice(0,5).map(p=><Text key={p.id}>{p.name}</Text>)}<Text>Recent ranked matches</Text>{matches.filter(x=>x.courtId===c.id).slice(0,3).map(mm=><Text key={mm.id}>{mm.score}</Text>)}<View style={{height:120,backgroundColor:'#efe3e8',borderRadius:12,marginVertical:12,justifyContent:'center',alignItems:'center'}}><Text>Map placeholder</Text></View><PremiumButton label='Book externally' onPress={()=>setM('External booking link opened')}/><PremiumButton label='Submit Match Here' onPress={()=>setM('Opened submit flow')} variant='secondary'/><PremiumButton label='Challenge Players Here' onPress={()=>setM('Challenge composer opened')}/><Modal visible={!!m} transparent><Pressable onPress={()=>setM('')} style={{flex:1,justifyContent:'flex-end',backgroundColor:'rgba(0,0,0,.3)'}}><View style={{backgroundColor:'#fff',padding:20}}><Text>{m}</Text></View></Pressable></Modal></ScrollView>}
