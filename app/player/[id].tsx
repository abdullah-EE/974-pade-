import { useLocalSearchParams } from 'expo-router';import { Image, Modal, Pressable, ScrollView, Text, View } from 'react-native';import { players,matches } from '@/data/mockData';import { useState } from 'react';import { PremiumButton } from '@/components/common/PremiumButton';
export default function Player(){const {id}=useLocalSearchParams();const p=players.find(x=>x.id===id)||players[0];const [m,setM]=useState('');return <ScrollView style={{flex:1,padding:16}}><View style={{alignItems:'center'}}><Image source={{uri:p.avatar}} style={{width:100,height:100,borderRadius:50}}/><Text style={{fontSize:24,fontWeight:'800'}}>{p.name}</Text><Text>{p.username}</Text><Text>Qatar Rank #{p.rank} • Rating {p.rating}</Text><Text>Favorite Court: {p.favoriteCourt}</Text><Text>Skill: {p.level}</Text></View><Text>Badges: Verified Player • Top 100 • Hot Streak</Text><Text style={{fontWeight:'700',marginTop:10}}>Recent matches</Text>{matches.slice(0,5).map(mm=><Text key={mm.id}>{mm.score}</Text>)}<PremiumButton label='Challenge' onPress={()=>setM('Challenge request sent')}/><PremiumButton label='Compare' secondary onPress={()=>setM('Comparison modal opened')}/><Modal visible={!!m} transparent><Pressable style={{flex:1,justifyContent:'center',alignItems:'center',backgroundColor:'rgba(0,0,0,.3)'}} onPress={()=>setM('')}><View style={{backgroundColor:'#fff',padding:20,borderRadius:12}}><Text>{m}</Text></View></Pressable></Modal></ScrollView>}
