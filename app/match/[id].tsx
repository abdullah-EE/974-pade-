import { useLocalSearchParams } from 'expo-router';import { ScrollView, Text, View } from 'react-native';import { useState } from 'react';import { matches, players, courts } from '@/data/mockData';import { PremiumButton } from '@/components/common/PremiumButton';
export default function MatchDetail(){const {id}=useLocalSearchParams();const m=matches.find(x=>x.id===id)||matches[0];const [st,setSt]=useState(m.status);return <ScrollView style={{flex:1,padding:16}}><Text style={{fontSize:24,fontWeight:'800'}}>Match Detail</Text><Text>Team A: {m.teamA.map(i=>players.find(p=>p.id===i)?.name).join(' / ')}</Text><Text>Team B: {m.teamB.map(i=>players.find(p=>p.id===i)?.name).join(' / ')}</Text><Text>Score: {m.score}</Text><Text>Winner: Team {m.winner}</Text><Text>Court: {courts.find(c=>c.id===m.courtId)?.name}</Text><Text>Date: {m.date}</Text><Text>Rating changes: ±{m.ratingChange}</Text><Text>Verification checklist: Score, GPS, Proof</Text><View style={{height:120,backgroundColor:'#eee',borderRadius:12,justifyContent:'center',alignItems:'center',marginVertical:12}}><Text>Proof image placeholder</Text></View><Text>Status: {st}</Text><PremiumButton label='Confirm Result' onPress={()=>setSt('Verified')}/><PremiumButton label='Dispute Result' secondary onPress={()=>setSt('Disputed')}/></ScrollView>}
