import { ScrollView, Text, View } from 'react-native';import { players, matches } from '@/data/mockData';import { colors } from '@/theme/tokens';
export default function Profile(){const me=players[3];return <ScrollView style={{flex:1,backgroundColor:'#F7F3F0'}}><View style={{backgroundColor:colors.primary,padding:18}}><Text style={{color:'#fff',fontSize:24,fontWeight:'800'}}>{me.name}</Text><Text style={{color:'#f4dfe9'}}>Rank #{me.rank} • Rating {me.rating}</Text></View><View style={{padding:16}}><Text>Record {me.wins}-{me.losses}</Text><Text>Win rate {Math.round(me.wins/(me.wins+me.losses)*100)}%</Text><Text style={{marginTop:10,fontWeight:'700'}}>Recent Matches</Text>{matches.slice(0,4).map(m=><Text key={m.id}>{m.score} • {m.status}</Text>)}</View></ScrollView>}
