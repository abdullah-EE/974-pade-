import { useState } from 'react';import { Modal, Pressable, ScrollView, Text, TextInput, View } from 'react-native';import { courts, players } from '@/data/mockData';import { PremiumButton } from '@/components/common/PremiumButton';
export default function Submit(){const [step,setStep]=useState(1);const [done,setDone]=useState(false);const [score,setScore]=useState('6-4 6-3');return <ScrollView style={{flex:1,padding:16}}><Text>Step {step}/4</Text>{step===1&&courts.slice(0,4).map(c=><Pressable key={c.id} onPress={()=>setStep(2)}><Text>{c.name}</Text></Pressable>)}{step===2&&players.slice(0,6).map(p=><Pressable key={p.id} onPress={()=>setStep(3)}><Text>{p.name}</Text></Pressable>)}{step===3&&<><TextInput value={score} onChangeText={setScore} style={{borderWidth:1,padding:10,marginVertical:10}}/><PremiumButton label='Continue to Proof' onPress={()=>setStep(4)}/></>}{step===4&&<><Text>Proof photo placeholder</Text><Text>GPS verification placeholder</Text><PremiumButton label='Submit Match' onPress={()=>setDone(true)}/></>}<Modal visible={done} transparent><Pressable onPress={()=>setDone(false)} style={{flex:1,justifyContent:'center',alignItems:'center',backgroundColor:'rgba(0,0,0,.3)'}}><View style={{backgroundColor:'#fff',padding:24,borderRadius:12}}><Text>Match pending confirmation.</Text></View></Pressable></Modal></ScrollView>}
