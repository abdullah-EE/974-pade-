import { useState } from 'react';import { ScrollView, Text, View } from 'react-native';import { challenges, players } from '@/data/mockData';import { PremiumButton } from '@/components/common/PremiumButton';
export default function Challenges(){const [items,setItems]=useState(challenges); const update=(id:string,status:any)=>setItems(v=>v.map(i=>i.id===id?{...i,status}:i));return <ScrollView style={{flex:1,padding:16}}><Text style={{fontSize:24,fontWeight:'800'}}>Challenges</Text>{items.map(c=><View key={c.id} style={{backgroundColor:'#fff',padding:12,borderRadius:10,marginTop:10}}><Text>{players.find(p=>p.id===c.from)?.name} vs {players.find(p=>p.id===c.to)?.name}</Text><Text>{c.status}</Text><View style={{flexDirection:'row',gap:8}}><PremiumButton label='Accept' onPress={()=>update(c.id,'Accepted')}/><PremiumButton label='Decline' secondary onPress={()=>update(c.id,'Sent')}/></View></View>)}</ScrollView>}
